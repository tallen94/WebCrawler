I implemented my crawler with multi-threading. It crawls the sitemaps using the thread queue rather than storing the urls. I also use the thread queue to crawl each url.

I created a graphical dashboard tallenwebcrawler.cloudapp.net